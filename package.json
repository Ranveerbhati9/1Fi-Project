{
  "name": "1fi-sde1-assignment",
  "version": "1.0.0",
  "description": "1Fi SDE1 Assignment - Mutual Fund Backed Product EMI Web Application",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "render-build": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "1fi",
    "mutual-funds",
    "emi",
    "react",
    "javascript",
    "express",
    "mongodb",
    "mongoose",
    "tailwind"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Ranveerbhati9/1fi_assigment.git"
  },
  "author": "Ranveer Bhati",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
